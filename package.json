{
  "name": "virtual-venus",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node scripts/fix-tool-routes.mjs && astro dev",
    "build": "node scripts/fix-tool-routes.mjs && node scripts/prepare-incremental-build.mjs && astro build && node scripts/inject-seo-content.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.2.6",
    "qrcode": "^1.5.4"
  },
  "allowScripts": {
    "esbuild": true
  }
}
